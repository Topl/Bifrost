name: admin
description: Bifrost admin console

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  topl_common:
    git:
      url: https://github.com/Topl/dart_topl_common.git
      ref: master
  fixnum: ^1.1.0
  fast_base58: ^0.2.1
  graphview: ^1.1.1
  async: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.3.3

flutter:
  uses-material-design: true

